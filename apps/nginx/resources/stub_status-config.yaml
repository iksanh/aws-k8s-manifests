apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-stub-status
  namespace: nginx
data:
  stub_status.conf: |
    server {
        listen 8080;
        location = /stub_status {
            stub_status;
        }
    }