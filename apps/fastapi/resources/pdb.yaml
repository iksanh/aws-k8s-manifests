apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: aws-k8s-app
  namespace: aws-k8s-app
spec:
  minAvailable: 1                    # minimal 1 pod always online
  selector:
    matchLabels:
      app: aws-k8s-app